{
  "name": "backend-medicare_booking_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker-compose up --build",
    "down": "docker-compose down",
    "stop": "docker-compose stop",
    "dev": "docker-compose up",
    "dev:api_gateway": "docker-compose up --build api-gateway",
    "dev:auth_service": "docker-compose up --build auth-service",
    "dev:user_service": "docker-compose up --build user-management-service",
    "dev:doctor_service": "docker-compose up --build doctor-management-service",
    "dev:appointment_service": "docker-compose up --build appointment-service",
    "dev:notification_service": "docker-compose up --build notification-service",
    "dev:payment_service": "docker-compose up --build payment-service",
    "dev:rating_service": "docker-compose up --build rating-service",
    "dev:mysql": "docker-compose up -d --build auth_db user_db"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/amqplib": "0.10.7",
    "tsconfig-paths": "4.2.0"
  },
  "dependencies": {
    "amqplib": "0.10.8"
  }
}
